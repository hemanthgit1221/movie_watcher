# FDFS Alert — Mobile

Phase 0 scaffold. Uses the **repo-local Flutter SDK** at `../../.tools/flutter` (see `../../scripts/flutter.ps1`).

```powershell
..\..\scripts\flutter.ps1 pub get
..\..\scripts\flutter.ps1 test
```
